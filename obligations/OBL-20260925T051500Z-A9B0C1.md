---
id: OBL-20260925T051500Z-A9B0C1
kind: followup
status: open
title: 'WR-3 self-review deferred hardening items: control bootstrap configure timeout + resolver/adapter latent edges (typed-bounded, none admission-blocking)'
statement: 'The v29 WR-3 admission self-review loop surfaced findings adjudicated as deferred (recorded, not fixed, to avoid creating control-repo commits beyond the admitted revision or resetting the approval count for latent nits): (1) qiven-workspace bootstrap/qiven-bootstrap.py defines CONFIGURE_TIMEOUT=900 but the configure subprocess.run call passes no timeout - a hung cmake --preset hangs the gate (availability class; outer supervision bounds it). (2) workspace_resolver.py adapter emits QIVEN_WORKSPACE_PROVIDER_CONTRACT_{pid} once per contract in a loop - multi-contract providers keep only the last value (no current provider declares >1 contract; no consumer reads the variable). (3) The adapter COMMAND-shape guard (if(NOT COMMAND qiven_workspace_materialize)) is first-definition-wins: a pre-defined same-named function suppresses the anti-spoof body (requires a governed-repo edit; review-caught class). (4) Adapter providers are built from direct target dependencies only - a transitive-only provider hits the typed FATAL_ERROR (fails closed/loud; no current edge triggers it). (5) _sanitize_id hyphen->underscore mapping can collide for ids differing only in separator (unreachable with the current node universe).'
why_it_exists: 'Each item was found by a fresh-review round, verified by the orchestrator, and adjudicated non-blocking for the WR-3 admission (typed-bounded or latent); fixing (1) in the v29 window would have created a control commit beyond the admitted revision fcfede8 requiring separate admission.'
why_not_now: 'The admission bound the exact control revision; the hardening batch rides the next natural control-repo change window (the WR-3 continuation / WR-4 lock transactions) so the fix rides an already-required control movement instead of forcing an extra admission cycle.'
scope: [qiven, devkit, workspace, resolver, bootstrap]
tags: [wr3, self-review-findings, deferred-hardening, typed-bounded, availability]
trigger:
  type: after
  value: 'The next control-repository change window (WR-3 continuation lock transaction or WR-4): fix (1) in the same control commit (NOT a routine advance - bootstrap change; needs its admission recorded per the ratified rule), and (2)-(5) in the devkit resolver as one bounded hardening batch when a non-first-party consumer or a multi-contract/transitive edge first becomes real, or at WR-5 adapter consolidation, whichever comes first.'
completion: 'Items (2)-(5) may close as WONTFIX-with-rationale if the adapter is redesigned before a trigger fires; item (1) must be fixed, not waived (hang class).'
created_at: '2026-09-25T05:15:00Z'
updated_at: '2026-09-25T05:15:00Z'
sources:
  - type: repository_file
    reference: 'D:/JasonWork/qiven-workspace/bootstrap/qiven-bootstrap.py (CONFIGURE_TIMEOUT dead constant, line ~32 vs subprocess.run without timeout, line ~169)'
  - type: chat_session
    reference: 'v29 session WR-3 self-review rounds 1/1''/1''''/2/3 final reports (workflow log 2026-09-25-v29-wr3-mvp4.md); findings F2, F5/F6/F7, R5 across rounds'
related: [ADR-0052, OBL-20260924T113000Z-E7F8A9]
supersedes: []
superseded_by: []
---

# WR-3 self-review deferred hardening items

None of these blocked the admission (three consecutive clean rounds
verified that); they are the honest residue with triggers so they
resurface instead of being forgotten.
