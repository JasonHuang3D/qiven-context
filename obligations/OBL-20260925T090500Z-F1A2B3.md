---
id: OBL-20260925T090500Z-F1A2B3
kind: followup
status: done
title: 'Next-session program: (1) subagent live boundary experiments with transcript provenance FIRST, (2) the CI background-runner prefix (owner design), (3) LLM-executed CI replacement proof - in that order, next session'
statement: 'Owner turn-2 direction 2026-09-25 (v29): the next session opens with LIVE EXPERIMENTS on the subagent delegation boundary, then builds the CI background runner, then the LLM runs the CI replacement proof itself. (1) Live experiments, FIRST ACTION, with clear provenance so the delegation design can be iterated: (a) transcript provenance - the harness persists full subagent conversation transcripts (the owner reads the thinking stream in the UI from the same source); harvesting and analysis must be MECHANICAL (a script scanning the transcript JSONL for pollution markers: loop-state terms, process-record mentions, round-position reasoning), never orchestrator eyeballs - probing whether the raw stream can ALSO be persisted live to a designated log file during the run is part of the experiment (candidates: post-hoc transcript harvest [raw, faithful] vs worker-appended self-report logs [procedural, self-censored]); (b) router-message boundary probe - a clean-context worker whose only mid-task correction channel is a hook-router denial: does the denial text (ADR references, exact re-call instructions) measurably cross the cognitive boundary; (c) clean-input A/B - the same review brief twice, once self-contained per the new Clean-input boundary vs once with a process-record reading list, compare findings/verdict drift. (2) CI background runner (owner design, replaces the historical human-manual-dispatch constraint whose premise - no auto-wake after turn end without human input - is now known false): a run_in_background python watcher in the gate pattern; internally it polls the DISPATCHED gh CI run every 10 seconds (pure OS process, zero token cost while running; the harness notifies once on completion); output stays clean - errors only. The runner OBSERVES; it does not change the dispatch-only trigger law (push never auto-runs CI; MEM-20260921T095600Z-C5E1A8 stands). (3) After the runner passes extensive tests + self-review + the live experiments, the LLM itself executes the CI replacement proof (the migrated runtime+math workspace-path CI) end to end.'
why_it_exists: 'The v29 pollution incident proved the clean-context property leaks through channels the spec did not govern (commit messages, terminal labels, process-record reading lists); the delegation design needs measured evidence, not assumption, before further reliance - and the CI proof chain needs a token-free observation primitive first.'
why_not_now: 'Owner scheduling: this session closes with the spec revision only; the live experiments are the next session FIRST step ("必须有一个明确的provenance才能反复打磨和修订我们的subagent的delegation").'
scope: [qiven, devkit, subagent-delegation, ci, runtime, math, harness]
tags: [live-experiment, transcript-provenance, router-boundary, clean-input, ci-runner, background-watcher, replacement-proof]
trigger:
  type: after
  value: 'The next session opening (before any engineering batch): live experiments first, then the CI runner batch, then the LLM-run CI replacement proof'
completion: '(Complete 2026-09-26, v31, long-running mode, delegated per-batch H2.) (1) Live-experiment evidence recorded and the delegation spec amended on it: evidence/audits/subagent-boundary-experiments-2026-09-26.md + scanner tools/scan_subagent_transcripts.py + subagent-delegation.md amendments + MEM-20260926T210000Z-A3B4C5 (verdict drift measured - process-record-laden briefs pollute 2/3 verdict lines; baits 6/6 clean; seeds 100%; ACK honesty 8/8; live hook denials 0 across 49 sessions; live thinking capture feasible). (2) The CI runner landed with tests + self-review: devkit main 90c675a (qiven ci watch - observation-only, identity-bound, 10s internal polling, JSON receipts, inherently terminating, explicit cross-repo identity mode; gate local PASS at cc7e769 and bb98dd4; 122-check operator suite; fresh-review round 1xP2+6xP3 all fixed + fresh-verify round). (3) The CI replacement proof executed by the LLM with receipts: math run 36185940592 SUCCESS at 0eb0aa0 AND runtime run 36185938457 SUCCESS at 7b3ce51 - dispatched via the repos pinned ci start (origin==local verified), observed to terminal state by the landed runner in explicit-identity background mode (receipts in the v31 checkpoint).'
created_at: '2026-09-25T09:05:00Z'
updated_at: '2026-09-26T21:30:00Z'
sources:
  - type: user_statement
    reference: '2026-09-25 owner turn-2 direction (v29): transcript-observed pollution evidence quoted; "立刻修订我们的subagent规范，只允许必要信息输入，并且严格禁止以任何方式获取其它辅助信息"; live tests as next-session first step with provenance; the CI background-runner design (10s polling, clean output, errors only) as the replacement-proof prefix; "这个会话不用做。下一个会话做。"'
related: [ADR-0053, MEM-20260925T090000Z-E2F3A4, MEM-20260921T095600Z-C5E1A8, MEM-20260924T231500Z-A5B6C7]
supersedes: []
superseded_by: []
---

# Next-session program: live boundary experiments, then CI runner, then LLM CI proof

Order is law: experiments before reliance; runner before proof. The
live-experiment transcript material must carry provenance (which task
files, which runs) so the delegation design can be re-iterated against
evidence rather than anecdote.
