---
id: OBL-20260918T215000Z-C9E3F1
kind: followup
status: done
title: Complete operator coverage of cmd/bat tools across Devkit-managed repositories
statement: 'Finish the Devkit operator conversion so that tools\qiven.cmd is the only cmd entry point in the managed set: convert the remaining managed cmd templates (resolve-toolchain, format, format-check, gen-vs2022-x64, delete-all-branches-but-main) to operator python tasks, self-host the operator inside qiven-devkit (its own .qiven/operator.json gate running the four suites), roll the updated managed set to qiven-foundation and qiven-math via sync, and assess qiven-context''s non-managed cmd tools for the same treatment. The apply-patch rename (ADR-recorded in devkit 46bb2f3 and foundation 5e93878) is the pattern.'
why_it_exists: 'The project owner directed that the operator cover all existing pure cmd/bat tools except the qiven.cmd launcher, because cmd wrappers duplicate orchestration semantics that Devkit owns and are a recurring control-flow hazard (operating-contract CMD rules, MEM-20260913T194500Z-8F2C41). The first slice (apply_patch.py, {python} placeholder, four suites green) proved the mechanism.'
why_not_now: 'The first slice landed incrementally with all devkit suites green; the remaining conversion is one designed batch that must roll managed sets through each repository''s own gate rather than being rushed alongside it.'
scope: [qiven-devkit, qiven-foundation, qiven-math, qiven-context-draft, operator, tooling]
tags: [operator, cmd-coverage, python-tasks, template-conversion, sync-roll, devkit-selfhost]
trigger:
  type: after
  value: 'ADR-0037 transaction acceptance (this design record)'
completion: 'Devkit self-hosts the operator (own .qiven/operator.json gate: check-toolchain, cmd-control-flow, adoption-missing-sync, operator-tests, devkit-regression, diff-check, clean-tree - all green); the five managed cmd templates were converted to operator python tasks (check_toolchain.py, format_sources.py, delete_all_branches.py plus the earlier apply_patch.py) with version pins moved to the toolchain.json manifest; the managed set rolled to 0.1.4 and gate-green in qiven-foundation (ab8606e), qiven-math (ac40c5d) and qiven-context-draft (ed13976, which also surfaced and fixed a Release-only assert-side-effect serialization bug - Debug-green is not acceptance). qiven-context assessment: its own cmd tools (bootstrap.cmd, test.cmd, validate.cmd and siblings) are task scripts referenced by its mature operator.json, not orchestration logic; conversion is recorded as out of scope for this batch and deferred until after K5, since churning continuity machinery during K5 prep buys nothing. tools\qiven.cmd is the only cmd entry in every Devkit-managed set.'

created_at: '2026-09-18T21:26:25Z'
updated_at: '2026-09-19T09:24:59Z'
sources:
  - type: user_statement
    reference: '2026-09-18 project owner directed operator coverage of all pure cmd/bat tools except tools\qiven.cmd, and a semantic rename of the apply-jason-brother script.'
  - type: git_commit
    reference: 'qiven-devkit 46bb2f3 feat(devkit): replace apply-jason-brother with the operator apply-patch task; qiven-foundation 5e93878 tools rename'
related: [ADR-0035, OBL-20260918T161352Z-C7E1A9]
supersedes: []
superseded_by: []
---

# Complete operator coverage of cmd/bat tools across Devkit-managed repositories

The first slice is landed and green: `apply-jason-brother.cmd` became the
operator `apply-patch` python task (`apply_patch.py`), the operator runtime
gained the `{python}` placeholder, and the managed manifest plus all four devkit
suites were updated together. This obligation tracks the remainder as one
designed batch: convert the five remaining managed cmd templates, self-host the
operator in qiven-devkit, roll foundation and math through sync, and give
qiven-context's own non-managed cmd tools the same decision. Every repository
lands through its own gate; a rolled repository that is not gate-green is not
done.
