# qiven-context

Qiven's durable project-cognition repository. This private repository is not a C++ library, devkit consumer, generic notebook, transcript dump, vector database, or service. It exists so current state, decisions, rejected alternatives, obligations, risks, assumptions, lessons, incidents, and provenance survive individual LLM sessions.

Phase 0 is active. Genesis historical import has **not** happened yet.
