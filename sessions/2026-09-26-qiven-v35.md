# Qiven-v35 Session Checkpoint

## Session identity

ZCode Desktop foreground session, owner-marked new session 2026-09-26
("这个是新的session"). Cold boot per BOOTSTRAP; designation
`jason-extended-cognition` owner-invoked at turn 1 with the live-model
switch report (GLM-5.3, provider account `bigmodel-offpeak-idle-plan`,
per harness disclosure; variant and reasoning effort not introspectable
from inside, reported as such). **Long-running mode from turn 1**
(owner instruction: "开启long-rurnning mode, 完整执行并落地OBL-B4C5D6，
直到必须的H1再停" — standing-delegated per-batch H2; H1-only stops).

## Exact current task

**EXECUTED TO THE NECESSARY H1:** OBL-20260926T234500Z-B4C5D6 items (4)
and (6) delivered end-to-end (items 1/2/3/5 were done in v33/v34).

1. The simulated ZCode hook lifecycle gate (ADR-0055): qiven-runtime
   `docs/design/mvp4-simulated-gate.md` (design `bba9133`, R3 anchor,
   precedes all semantics commits), rig `tools/h1_sim_gate.py` +
   `tests/fixtures/h1-sim/` (catalogue with 3 provenance classes +
   oracle bindings + the digest-pinned invocation-policy.yaml copy).
   104 named scenarios / INV-1..18 / five incidents distinct
   old-fail/new-pass; wire fault client (DPAPI unprotect + HMAC
   framing per src/ipc/framing.cpp) for trial-3/4 mechanisms and
   shutdown; writable-child negative control; 6 detector limits
   recorded; skip acceptance-fatal; receipt bound to the exact head;
   verify-receipt rejects dev/stale/failed. Gate wiring: `h1-sim` +
   `h1-kit-test` in gate:local (operator.json).
2. Folded fixes: trial-4 hello-deadline (hello = handshake ≤5000 ms,
   `hello_ceiling_ms`); h1_kit.py preflight session_start coverage;
   h1_kit_test.py skip→NOT_RUN-fail.
3. Gate-detected defect (ADR-0055 d10) fixed at its semantic owner:
   governed exact-FILE targets never matched; fixed root-bounded
   (`starts_with_ci`/`ends_with_ci` + root prefix bound;
   containment-anywhere kept as labeled over-approximation). Lesson
   MEM-20260927T003000Z-E4F5A6. Old-fail receipts preserved (the dev
   receipts of runs 1-3); C++ suite 41/41 green.
4. Iteration record (honest): first dev run 55 failures → 8 root
   causes classified (mostly rig-oracle errors: wire admission,
   outside-root lexical containment, silent-allow paths, JSON append
   position, pre-existing fixture files in effect asserts; ONE real
   host defect); rounds 55→22→8→0.
5. Published (delegated per-batch H2): branch
   `jason-extended-cognition/mvp4-simulated-gate` → main `b08bf99`
   (gate:local PASS at `d6f2cf0`, 12 tasks, h1-sim 25.75s); rig setup
   containment `aa2076e` (gate:local PASS at head; found by control
   experiment EXP-2). Gate-grade receipts:
   `sim-d6f2cf066bdd-20260926T161241Z.json`,
   `sim-aa2076e201d3-20260926T161640Z.json` (104/104, 0 skipped,
   verify-receipt OK at the live head). Workspace lock: batch-internal
   advances `5a4143b` + (aa2076e folded), conform `6469978` →
   `a7d16d1`; generation `sha256:8c70aab4…`; the standing
   context→devkit pin [SPLIT] unchanged (WR-6).
6. Item 6: `views/workflows/r2r3-continuity-checklist.md` (PR-6
   interim: source-bound activation/acceptance receipts,
   compact-boundary + reviewer-brief observation rows, invariant→test
   map). Control experiments EXECUTED: EXP-1 lossy-summary replay
   (degraded statement → activation receipt INVALID on 4/5 fields →
   loss surfaced); EXP-2 seeded old-code error (publication gate
   REJECTS the pre-fix binaries, typed trial-4 mechanism text; also
   found the rig setup-containment defect, fixed + published).

## Accepted refs and evidence

- qiven-runtime main `aa2076e` (pushed; history `b08bf99` ← `d6f2cf0`
  ← `1107d3e` ← `bba9133` ← 8da3104).
- Gate receipts under qiven-runtime `.generated-temp/h1-sim/receipts/`
  (gate-grade: sim-d6f2cf066bdd…, sim-aa2076e201d3…; experiment FAIL
  receipts + exp2 log preserved; old-fail-i4 PASS-mode evidence).
- Pre-fix reference binaries preserved at
  qiven-runtime `.generated-temp/h1-sim/prefix-reference/` (host
  b9a6036c…, hook 530a733d…, ctl 2f8317cb… digests recorded) — the
  I4 old-fail pair; regenerable from git history (the v34-era build).
- qiven-workspace main `a7d16d1` (lock generation `8c70aab4…`).
- v35 workflow log: `<workspace>/.generated-temp/workflow-logs/
  v35-obl-b4c5d6.md` (experiments, receipts, stash).

## The necessary H1 (session stops here)

The owner adjudicates:

1. **The `MVP4_SIMULATION_ACCEPTED` MVP-4 exit record** per ADR-0055 —
   the gate is green at the published head with non-skipped receipts
   and the standing `INSTALLED_DESKTOP_EXECUTION_UNVERIFIED` residual.
   Accepting it closes OBL-B4C5D6's completion criteria (all other
   items delivered).
2. **The next boundary:** RR-0 mechanical batch → CA-1 (may interleave
   now that the simulated gate has landed), or the WR-5/WR-6 pre-wave
   (devkit template/tool pre-wave → WR-5 owner opening grant → WR-6
   pin reconciliation).

Everything needed for both decisions is machine-verifiable from the
receipts above; no paste-ready kit is required (plain adjudication on
published evidence).

## Unaccepted candidate refs

- None open in qiven-runtime (both publications merged after gate PASS
  at exact heads under delegated H2).
- THIS context transaction (checklist + MEM + OBL/state riders + this
  checkpoint) rides the same delegated H2: publication = branch →
  context-tools gate PASS at the exact head → merge to main → push.

## Pending asynchronous work

- None running. (All builds/gates completed; no straggler probes.)

## Known inconsistencies and evidence gaps

- The standing qiven-context devkit pin (d1d2a3a) vs lock devkit node
  typed [SPLIT] — unchanged, WR-6 target (recorded in every lock
  receipt).
- Session records for Qiven-v2..v5 remain missing (carried; do not
  synthesize). ZCode binary-vs-pinned-tag drift caveat stands (v31).
- The rig's detector-limits list (B1/B8 capture-probe targets, bash
  backslash form, dot-segment, double separator, junction/reparse) is
  HONEST coverage-gap recording per the profile's detector-scope
  honesty note — MVP-7 dogfood territory, not claimed as mediation.
- The prefix-reference binaries live under .generated-temp
  (regenerable class); the durable old-fail evidence is the git
  history + the receipts + the audit references.

## Standing continuity note

Canonical records are authoritative; this checkpoint is bounded
continuity evidence only. The PR-6 interim checklist now binds every
R2/R3 batch (activation receipt before design; acceptance receipt
before publication; compact boundary invalidates the activation).

## Next action

Session STOPS at the owner H1 above. A fresh session cold-boots to the
owner's adjudication + next-boundary choice (state/current.md Next
boundary; active-work.yaml current_candidate).
