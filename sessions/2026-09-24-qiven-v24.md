# Qiven-v24 Session Checkpoint

## Session identity

ZCode foreground session (local start 2026-09-24 ~04:46 CST, UTC
2026-09-23T20:46Z). New session owner-marked (verbatim: "这个是新的session").
Designation `jason-extended-cognition` owner-invoked at the first turn.
**Long-running mode ENTERED at the first turn** (owner direction, verbatim:
"开启long-running mode，全程委托你H2"): per-batch H2 standing-delegated per
`views/workflows/long-running.md`; H1/H3/H4 unchanged; governance mutation,
ADR acceptance and qualification upgrades remain owner H2.

Live-model switch report (view binding policy, owner-invoked designation):
GLM-5.3, provider account `bigmodel-offpeak-idle-plan` (same account
routing as v23); flash/non-flash variant and reasoning effort not
introspectable, reported as such.

## Exact current task

Owner-delegated diagnosis + ADR + landing of the token-economy /
long-command execution architecture ("exec 仍不可用" + the exec-detour
self-reflection), executed to completion under delegated per-batch H2.

## Diagnosis summary (evidence in ADR-0051 sources)

1. **exec leak recurrence**: NOT an exec v2 failure — qiven-runtime was
   still pinned to the v1 operator snapshot (template 0.1.8); the five
   v23 MSBuild builds there (records 20260923T1939-1946Z, no custody
   identity) ran unbounded (MSBuild node reuse, no Job Object). v2
   works (devkit receipts clean). v23 violated OBL-…-B9C1D3's trigger
   condition (heavy exec usage in runtime before sync).
2. **exec-detour token defect (owner self-diagnosis CONFIRMED and
   sharpened)**: polling supervision costs O(polls × live-context) per
   long command; the harness natively provides the fix
   (run_in_background + one completion notification; >~25-30KB output
   auto-persists with ~2KB preview). Two contradictory instruction
   layers (user-level background ban vs workspace exec detour) taxed
   every retry.

## Accepted refs and evidence

(All under delegated per-batch H2.)

- **qiven-devkit PR #31 → `1efbd2f`** (batch head `901f0a8`): hook
  router v4 — deny→`run_in_background` re-call teaching; build/gate
  node-reuse guard (`MSBUILDDISABLENODEREUSE=1` / `/nr:false`);
  long-class split build/sweep/repo-tool/network; sweeps stay
  exec-lease; 13 new end-to-end v4 cases (73 classification + 9 probe
  total). Gate local PASS receipt
  `local-901f0a8b3f4dcb8f66a1b577386520586c75f2fa.json` (10/10),
  custody run 20260923T212854Z exit 0. operator-usage.md v4 section.
- **qiven-runtime PR #58 → `d1345b2`** (batch head `48ce3a6`): managed
  snapshot 0.1.8→0.1.9 (exec v2 custody: watchdog + KILL_ON_JOB_CLOSE
  Job Object + lease + completion reap) + foundation pin
  79e6789→b15d17c + cognition_pin.cpp per-process temp fixtures
  (root-caused the v23 "transient" suite flake: fixed temp names race
  under test-debug ∥ test-release, MEM-20260923T215200Z-C3D4E5; two
  deterministic in-gate reproductions, isolated + single-config always
  green). Gate local PASS receipt `local-48ce3a6e...json` (11/11).
  Closes the runtime row of OBL-20260923T234500Z-B9C1D3.
- **qiven-context (this PR)**: ADR-0051 PROPOSED + registry/contract
  amendments + MEM-…-A1B2C3/B2C3D4/C3D4E5 + OBL-…-D5E6F7 (background
  lifetime characterization) + this checkpoint + state updates.
- **User-level (outside git, disclosed)**: `run_in_background` block
  hook registration REMOVED from `C:/Users/61626/.zcode/cli/config.json`
  (it was found already `"enabled": false` — likely owner-disabled in
  the UI during this session; the mjs file remains on disk as evidence);
  global `C:/Users/61626/.zcode/AGENTS.md` rewritten to the ADR-0051
  law. Both take effect for sessions started after the change.

## Unaccepted candidate refs

- **ADR-0051 is PROPOSED — owner H2 ratification pending** (ADR
  acceptance is non-delegable). The mechanical landing is complete and
  live; rejection rolls back the three PRs + the user-level files.

## Pending asynchronous work

- None dispatched. CI remains explicit-dispatch-only.

## Known inconsistencies and evidence gaps

- **Hook-config reload semantics corrected live**: the workspace router
  script (a live path into the devkit tree) executes FRESH CONTENT per
  tool call — v4 was enforced inside the authoring session (two
  deliberate self-catches: heredoc python - <<EOF denied; `ctest` raw
  and `grep -rn` denied with v4 teaching messages). Registrations
  (which hooks exist) load at session start. The MVP-4 kit GUIDE's
  "hooks load at session start only" claim is about REGISTRATION, not
  script content — worth a wording pass in the kit standard.
- **Transitional layer contradiction (resolved next session)**: this
  session still ran with the user-level background ban registered at
  start, so build-class calls had to use the exec transitional path
  (sanctioned; ADR-0051's own messages said "do NOT detour through
  exec" — the exec exemption in the router kept it legal).
- **Process slips recorded (contract: record if material)**: one
  `python -c` inline file edit (foundation pin bump) — file-authoring
  law slip (native Edit used for everything after; format-check and
  tests unaffected); one double-cmd exec invocation misfire (corrected
  to the python entry; no state impact).
- Standing (carried): session records for Qiven-v2 through Qiven-v5 remain
  missing (Do not synthesize them); ContextView<ChatGPT, Jason> remains
  operational for ChatGPT sessions; governance audit due after five ADRs
  since 2026-09-22 (0047/0048/0049/0050 = four; ADR-0051 would be the
  fifth IF accepted); sibling operator snapshots (foundation, math,
  toolchain-win, third-party-win) still v1 (OBL-…-B9C1D3); managed
  template syncs still need manual operator.json customs per bump;
  devkit router git-suspension flip-back owner-discretionary;
  RR-0 implementation + accept-vanish regression open in the CA
  interval; additionalContext live injection owner-enable-gated.

## Next action

> **v25 correction (2026-09-24):** the decision point and rollback map
> below are superseded by `sessions/2026-09-24-qiven-v25.md` — the
> user-level layer this session "deconflicted" against was an
> accidental configuration (owner-deleted), ADR-0051 was re-reviewed
> and amended in v25, and the rollback set changed (sibling custody
> rolls do NOT roll back with the ADR). Kept as history.

**Owner H2 decision point (paste-ready):** ADR-0051 ratification.
Reply one of:

- 批准 ADR-0051 (→ set status accepted + index + this checkpoint; then
  the next boundary stays as v23 declared: MVP-4 real H1 rerun at
  `h1-kits/qiven-runtime/mvp4-h1/0.1.0-g6ebf6ff6` — now unblocked from
  the exec-leak concern since runtime runs v2 custody)
- 拒绝 ADR-0051 (→ roll back: revert qiven-context PR (this batch),
  revert devkit PR #31, revert runtime PR #58, restore
  `C:/Users/61626/.zcode/cli/config.json` block registration +
  old AGENTS.md — the mjs file is intact on disk)

Then: the first fresh session runs the OBL-20260923T215500Z-D5E6F7
verification probes (background notification, TaskStop tree-kill,
timeout binding; see the obligation file) before relying on the
background path beyond guarded build classes.
