# Qiven-v20 Session Checkpoint

## Session identity

ZCode foreground session under the owner-granted designation
`jason-extended-cognition` (explicit owner instruction at open).
**Long-running mode ENTERED this session** by owner delegation
("开启long-running mode，全程委托你H2"): per-batch H2
standing-delegated with receipts; governance/ADR acceptance remains
owner H2. Scope: full review of devkit tooling, complete resolution of
the post-v19 process-leak incident, exec architecture rebuild to
production grade, devkit docs Python standard, and the closing
governance transaction.

Live-model switch report (view binding policy, owner-invoked
designation): harness reports GLM-5.3 (provider account
`bigmodel-offpeak-idle-plan`, same as v17/v18/v19); flash/non-flash
variant and reasoning-effort setting are not introspectable from inside
the session and are reported as such.

## Exact current task

Owner emergency instruction (2026-09-23): the v19 session left dozens
of msbuild/cmd processes and a ghost find.exe burning ~70% CPU past
session end and IDE exit (OS restart required); exec has repeatedly
failed to stabilize; tear the architecture down and rebuild it once,
to production grade, without waiting for the ADR process (ADR drafted
after the work). COMPLETED:

1. **Cold boot + full review**: canonical context, devkit docs (all),
   devkit tools (all), exec record stores (139 runs in qiven-runtime
   during the incident window), incident root-cause analysis.
2. **Design-first** (`qiven-devkit/docs/design/exec-custody.md`):
   bounded process custody — watchdog custodian per exec run holding a
   named KILL_ON_JOB_CLOSE Job Object it joins itself; hard lease
   (--max-lifetime, default 3600s); completion reap (1.5s grace); task
   custody inline (suspended-create → assign → resume); sweep
   piggybacked on every invocation; invariants I1-I8.
3. **Implementation** (devkit `dd5843f`, PR #27 merge `d1d2a3a`):
   exec v2 + gate/task custody + `_winapi` spawn channel + atomic
   records with reader-collision retry. Three live-found concurrency
   defects fixed inside the batch (env-block WinError 87; fd-vs-handle
   stdio loss; supervisor-pipe inheritance blocking; replace-vs-reader
   crash of a live watchdog) — each now a named regression class.
4. **Hook router**: segments lstripped (closes
   OBL-20260923T224500Z-A7B8C9); tree-sweep long-class added (the
   ghost find.exe channel: find path-forms, grep -r, dir /s; Windows
   filter find stays raw).
5. **Tests to production grade**: operator suite rewritten as 89 named
   checks (custody classes C1-C10: node-reuse reap, lease enforcement,
   custodian-death kernel kill, stop of job-orphaned members, task-tree
   reap, sweep, atomic records, custody identity, heartbeat freshness;
   all v1 regressions preserved) — green on 4 consecutive runs (flake
   law); router 73 classification + 9 probe + provenance cases green.
   Full devkit gate:local PASS at exact head `dd5843f` — executed
   through the new exec custody itself (dogfood).
6. **Python standard**: `qiven-devkit/docs/engineering/python-standard.md`
   (Devkit-canonical; incident-derived Windows spawn law, custody law,
   error honesty, concurrency, output discipline, determinism, tool
   self-tests) + engineering README index.
7. **Rollout**: qiven-context shim pin → `d1d2a3a` (PR #120 merge
   `132b420`, gate:context-local PASS at exact head `2f15ad1`). Sibling
   snapshots recorded as OBL-20260923T234500Z-B9C1D3.
8. **Governance transaction (this commit)**: incident audit
   (`evidence/audits/2026-09-23-exec-process-leak-incident.md`), memory
   records MEM-20260923T233000Z-E7F8A1 (custody law) +
   MEM-20260923T233500Z-F8A9B2 (Windows spawn/file scar tissue) +
   index, OBL-A7B8C9 closed, long-command-registry tree-sweep class +
   whitespace-tolerance note, ADR-0048 PROPOSED + index, state/current +
   active-work converged, this checkpoint.

## Accepted refs and evidence

- qiven-devkit: PR #27 merge `d1d2a3a` (batch head `dd5843f`,
  gate:local PASS receipt at exact head; delegated-H2 receipt in the
  PR body). Local main == origin/main, verified.
- qiven-context: PR #120 merge `132b420` (pin head `2f15ad1`,
  gate:context-local PASS receipt; delegated-H2 receipt in the PR) +
  this transaction.
- Delegated-H2 receipts per the long-running mode; no governance
  mutation merged (ADR-0048 stays PROPOSED — owner H2).

## Unaccepted candidate refs

- None open.

## Pending asynchronous work

- None. No CI dispatched (platform-first local acceptance).

## Known inconsistencies and evidence gaps

- Standing (carried from v19): session records for
  Qiven-v2 through Qiven-v5 remain missing (Do not synthesize them);
  ContextView<ChatGPT, Jason> remains operational for ChatGPT
  sessions; managed template syncs need manual operator.json customs
  per bump; governance audit due after five ADRs accepted since
  2026-09-22 (ADR-0047 is one; ADR-0048 is proposed, not counted).
- The workspace ZCode hook (`.zcode/config.json`) is owner-DISABLED
  during this session (owner action at open); the fixed router is
  merged but not armed until the owner re-enables it. Re-enabling is
  an owner action (the session must not silently re-arm the owner's
  IDE configuration).
- Sibling repositories still carry the v1 operator snapshot until
  OBL-20260923T234500Z-B9C1D3 rolls (their exec keeps the leak class
  in the meantime).
- One process-disclosure: two mechanical multi-site substitutions in
  devkit Python sources during the batch were executed via short
  python compute scripts (identical-string substitutions, the
  apply_patch.py class) rather than native Edit; content verified
  byte-exact afterward; native tools used for all other authoring.
  Also one exploratory heredoc probe script was used for isolating the
  WinError 87 spawn failure (compute/diagnosis, not file authorship —
  recorded here for completeness under the no-silent-detour duty).
- ADR-0048 acceptance, governance audit standing count, and MVP-4
  start remain owner decisions.

## Next action

Owner H2/H1 decisions, in order of urgency: (1) accept ADR-0048
(bounded process custody law); (2) re-enable the workspace hook to arm
the fixed router; (3) direct MVP-4 start (unchanged baseline: runtime
`0044fe1`, foundation `79e6789`, devkit `d1d2a3a`, draft `7583724`,
math `33cf0c2`, singleton `3184538`; context at this transaction's
merge). Session v20 closes with this transaction; a fresh session
cold-boots from the merged heads. Long-running delegation is
per-session — a fresh session needs fresh owner delegation. Nothing
else is owed by any party.
