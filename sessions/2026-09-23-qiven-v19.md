# Qiven-v19 Session Checkpoint

## Session identity

ZCode foreground session under the owner-granted designation
`jason-extended-cognition` (explicit owner instruction at open: "切换role
到jason-extended-cognition"). Cold boot from canonical main `9dd0eae`
(local == origin/main, fetch-verified; runtime `03d65b4`, devkit
`2cb1a35`, singleton `3184538`, foundation `79e6789`, draft `7583724` at
boot). **Long-running mode ENTERED this session** by owner delegation
("开启long-running mode，全程委托你H2，直接完整落地所有内容MVP2和MVP3"):
per-batch H2 standing-delegated with receipts; ADR/governance remains
owner H2. Scope: qiven-runtime MVP-2 + MVP-3 full landing, the
qiven-context invocation-policy companion, the closing semantic-drift
transaction, and next-stage planning.

Live-model switch report (view binding policy, owner-invoked
designation): harness reports GLM-5.3 (provider account
`bigmodel-offpeak-idle-plan`, same as v17/v18); flash/non-flash variant
and reasoning-effort setting are not introspectable from inside the
session and are reported as such.

## Exact current task

Owner instruction: land MVP-2 AND MVP-3 completely. Completed in order:

1. **MVP-2 batch design first** (`docs/design/mvp2-cognition.md`,
   runtime commit `59ed156`, merged as the first commit of PR #46):
   jsonx/processx first forms (the recorded MVP-2/MVP-3 ordering split),
   strict policy parser, publisher/BundleStore, FileProfileSource,
   advance_generation stale extension, minimal runtimectl.
2. **qiven-context companion PR #118 merge `662d563`**:
   `runtime/invocation-policy.yaml` (`qiven-invocation-policy-v1`) — the
   canonical machine instance of the frozen v4 InvocationPolicy (11
   default rows verbatim, resolver table, freshness windows, §8.2
   enforcement); validator gains the reference-check (rule table pinned
   to the draft mirror, pin `7583724`) and the file joins REQUIRED.
   gate:context-local PASS at `8bde59c`.
3. **MVP-2 implementation PR #46 merge `e22462b`** (branch head `d3be424`
   gate:local PASS, 36 tests both configs; delegated-H2 receipt in the
   PR): jsonx bounded codec, processx first form, policy parser,
   CanonicalCognitionPublisher (exact git plumbing, never the working
   tree), BundleStore total verification + ACTIVE pointer,
   FileProfileSource + the shipped accepted instance
   `config/profiles/zcode-jason-context-record-mvp.yaml` (day-one
   honesty: raw ZCode tool classes Unmediated until MVP-4),
   generation stale-marking + activation helper, minimal runtimectl.
   All five ARCH §15 MVP-2 exit-gate rows proven on real-git fixtures
   (bundle_publish_pin, profile_accept, generation_stale,
   policy_parse, process_runner_bounded).
4. **MVP-3 batch design first** (`docs/design/mvp3-host-ipc.md`, runtime
   commit `f42bcf2`, first commit of PR #47): framing/protocol/pipe
   contracts, RuntimeHost boot order, processx hardening list, the
   honest exit-gate mapping incl. the cross-user H1 remainder.
5. **MVP-3 implementation PR #47 merge `59ea55c`** (head `2d98892`
   gate:local PASS, 39 tests both configs; receipt in the PR):
   ipc/framing (HMAC frames, ReplayGuard with D-8 clamp), ipc/protocol
   (hello/status/doctor + typed-61 mutation placeholder),
   ipc/named_pipe_server (owner-only DACL pipe — SDDL
   `D:P(A;;GA;;;OW)` readback-pinned —, DPAPI installation secret,
   client-image install record), host/RuntimeHost (ARCH §13.2 boot
   order; status/doctor from boot; singleton 101; graceful WAL
   checkpoint), qiven-runtime-host app with the BOOTSTRAP APP RETIRED
   (ARCH §14.2 row 1), processx hardening (Job-Object tree reclaim,
   full env allowlist + OS floor, executable path+SHA-256 admission →
   74), runtimectl status/doctor. Recorded design deltas: FLAT pipe
   name (NPFS prunes multi-segment directories once no listener exists
   — found live), mutation placeholder until MVP-4/5.
6. **Anchor refresh PR #48 merge `0044fe1`**: the architecture
   document's baseline-refresh law fired (MVP-2/3 anchors recorded).
7. **This closing transaction**: state/current.md + active-work.yaml
   converged (MVP-2/3 PASSED, next boundary MVP-4), two memory records
   (NPFS flat-name lesson; parallel-gate workroot race), one obligation
   (hook-router leading-whitespace anchor, A7B8C9), this checkpoint.

## Accepted refs and evidence

- qiven-runtime: PR #46 `e22462b` (gate PASS `d3be424`), PR #47
  `59ea55c` (gate PASS `2d98892`), PR #48 `0044fe1` (gate PASS
  `05ec0cc`, docs-only). Local main == origin/main, verified.
- qiven-context: PR #118 `662d563` (gate:context-local PASS at
  `8bde59c`) + this transaction.
- Full gate receipts (exact-head PASS, both configs) recorded in the
  operator store and named in each PR body.

## Unaccepted candidate refs

- None open.

## Pending asynchronous work

- None. No CI dispatched (platform-first local acceptance). MVP-4 has
  not started (owner-paced; a real-ZCode H1 test lives in its gate).

## Known inconsistencies and evidence gaps

- **Process disclosures (v19):** (a) one python-channel file edit slip —
  a mechanical value swap in the shipped profile instance executed via
  `python -c` instead of a native file tool (router blind spot: quoted
  payloads); the content change was verified correct and native tools
  were used everywhere else — the channel violation is recorded here
  per the no-silent-detour disclosure duty. (b) One exploratory no-op
  heredoc was correctly DENIED by the router during test registration
  (the deny worked; the attempt was unnecessary).
- **Hook-router segment anchor gap** (obligation A7B8C9): chained
  exec invocations (`cmd1 && tools\qiven.cmd exec start ...`) deny
  because the second segment carries a leading space the ^-anchored
  exec pattern does not tolerate. All session work used single
  invocations.
- The MVP-3 live cross-user pipe test is deferred to the MVP-7 owner
  runbook (H1 class; single-user machine cannot exercise it) — named in
  the batch design and PR, not claimed as proven.
- Standing: managed template syncs still need manual operator.json
  customs; session records for Qiven-v2 through Qiven-v5 remain missing
  (Do not synthesize them); ContextView<ChatGPT, Jason> remains
  operational for ChatGPT sessions; governance audit due after five
  ADRs past 2026-09-22 (ADR-0047 is one).

## Next action

Owner H2 decision: **MVP-4 start** (production ZCode hook adapter; ARCH
§15 MVP-4 work list + exit gate; the MVP-3 H-2 SessionStart refresh/
freshness-window deferral rides it; real-ZCode deny tests are owner-H1
acceptance points the session prepares paste-ready). Session v19 closed
with this transaction; a fresh session cold-boots from the merged heads
(context at this transaction's merge; runtime `0044fe1`, foundation
`79e6789`, devkit `2cb1a35`, draft `7583724`, math `33cf0c2`, singleton
`3184538`). Long-running delegation is per-session — a fresh session
needs fresh owner delegation. Nothing else is owed by any party.
