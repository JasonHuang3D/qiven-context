# Qiven-v25 Session Checkpoint

## Session identity

ZCode foreground session (local start 2026-09-24, owner-marked new
session). Designation `jason-extended-cognition` owner-invoked at the
first turn. **Long-running mode ENTERED at the first turn** (owner
direction, verbatim: "开启long-running mode，全程委托你H2"): per-batch
H2 standing-delegated per `views/workflows/long-running.md`; H1/H3/H4
unchanged; governance mutation, ADR acceptance and qualification
upgrades remain owner H2.

Live-model switch report (view binding policy, owner-invoked
designation): GLM-5.3, provider account `bigmodel-offpeak-idle-plan`
(same account routing as v23/v24); flash/non-flash variant and
reasoning effort not introspectable, reported as such.

## Exact current task

Owner-delegated (verbatim class): re-review the v24 ADR-0051 draft
from a "全局专业且客观" standpoint because the owner suspected noise
impaired the v24 judgment; achieve FULL-WORKSPACE operator coverage
for every `qiven-*` repository under D:/JasonWork ("不能再出现这个问
问题"); diagnose, amend, land to root cure; ratification reserved for
the owner next turn.

## ADR-0051 re-review verdict (the noise round)

The owner's four framing questions, answered from evidence:

1. **"以前定过让 LLM 主动轮询长任务状态，很贵"** — CONFIRMED. The
   2026-09-23 registry/router-v3 taught deny→`qiven exec start`→poll
   `qiven exec status`; every poll re-sends the live context
   (O(polls × context)). ADR-0051's core diagnosis is correct.
2. **"exec 也是希望这种方式？"** — NO, post-ADR-0051: exec is scoped
   to custody classes only (survival-requiring runs, owner kits,
   sweeps, >10-min Bash-ceiling work). Ordinary in-session long work
   goes background. `qiven exec/info/status` stay raw — no forced
   polling anywhere in the taught paths.
3. **"exec 其实是做了 run_in_background 的事情？"** — Partially
   overlapping but NOT redundant: exec v2 adds what background lacks
   (session-independent survival, watchdog + Job Object + lease,
   durable receipts). The v24 defect was exec being the DEFAULT path,
   not its existence. The ADR's custody-class split is correct and
   kept.
4. **"钩子在 pre tool call 拒绝 + 让 LLM 用 run_in_background 重调？"**
   — That IS router v4's design, and this session proved the loop
   naturally (P5: raw gate call denied with exact teaching → guarded
   background re-call → one completion notification).

**Noise points found and corrected (5):**

- N1: v24's "instruction-layer deconfliction" (decision 7) treated the
  user-level hook + global AGENTS.md as a deliberate standing layer —
  the owner clarified it was an ACCIDENTAL config, deleted personally.
  ADR amended: law carriers = canonical registry + operating contract
  + workspace router; nothing outside qiven-governed surfaces.
- N2: OBL-B9C1D3's inventory was wrong — toolchain-win/third-party-win
  carry NO operator snapshot; **context-draft was missing from every
  list** while carrying a customized v1 snapshot. Inventory corrected;
  rollout completed; obligation closed.
- N3: the v24 R1 mitigation "the Bash timeout parameter bounds calls"
  was FALSE for the background path — probed live (P3: 45s task ran
  natural under timeout=6000). Withdrawn; corrected wording landed.
- N4: platform facts conflated probed vs documented — P1/P2/P5 now
  proven fresh-session; P4 (session-end) explicitly open.
- N5: router v4's `_GATE_CLASS` anchor did not tolerate the env-assign
  prefix of its own taught re-call form — the guard check was vacuous
  for the taught path and any `FOO=1 qiven gate` bypassed the class
  raw (found live this session: a preemptively-guarded foundation
  gate call ran unclassified). Fixed in router v4.1 (devkit PR #32)
  with regression cases.

**Overall verdict: ADR-0051's architecture is sound; the v25
amendments remove the noise. Ratification recommended.**

## Accepted refs and evidence

All under delegated per-batch H2:

- **qiven-foundation PR #20 → merge `f28b86b`** (batch head
  `0942cfe`): template 0.1.8→0.1.9 operator sync. Gate local PASS at
  exact head (receipt `local-0942cfe89f56...json`, 2026-09-23T22:50:06Z).
- **qiven-math PR #12 → merge `518e511`** (batch head `a461c84`):
  template sync + foundation re-pin f28b86b. Gate PASS (receipt
  `local-a461c84fe835...json`, 22:53:02Z).
- **qiven-context-draft PR #40 → merge `ea9af72`** (batch head
  `054dd38`): template sync (operator.json customs re-applied
  byte-identical; the 0.1.8 rendered baseline was reconstructed from
  devkit git history to pass the sync's conflict check — the
  documented convention) + foundation re-pin. Gate PASS (receipt
  `local-054dd38ebd6b...json`, 22:53:04Z).
- **qiven-runtime PR #59 → merge `fc2d8a9`** (batch head `01505ab`):
  pin ripple — re-pins foundation f28b86b AND draft ea9af72 (runtime's
  gate failed twice mid-batch until both upstream merges landed,
  proving the pin graph: runtime→{foundation, draft}). Gate PASS
  (receipt `local-01505ab40168...json`, 22:54:58Z, 10 tasks).
- **qiven-devkit PR #32 → merge `223eaaa`** (batch head `7d01b57`):
  router v4.1 env-prefix fix + 6 regression cases (self-test
  76 classification + 16 v4 cases PASS). Gate PASS (receipt
  `local-7d01b57c128a...json`, 22:57:05Z).
- **qiven-context (this batch)**: ADR-0051 v25 amendments; OBL-B9C1D3
  closed + OBL-D5E6F7 narrowed (index updated); registry + operating
  contract v25 corrections; MEM-20260924T231500Z-A5B6C7 (probe facts)
  + MEM-20260924T232000Z-B6C7D8 (inventory-noise + pin-ripple
  lessons); state updates; this checkpoint.
- All gates ran via the ADR-0051 taught path (deny → background
  re-call + node-reuse guard, one notification each).

## Probe results (OBL-20260923T215500Z-D5E6F7)

- **P1 PASS** — benign background task: immediate output-file path,
  exactly ONE completion notification at exit 0.
- **P2 PASS** — TaskStop kills the whole tree (python parent 20216 +
  spawned ping child 13588 both dead by tasklist; one killed
  notification).
- **P3 FINDING** — the Bash `timeout` parameter does NOT bind
  background tasks (45s natural completion under timeout=6000).
- **P5 PASS** — the natural deny→re-call flow works end to end (also
  observed organically for two sweep-class denials this session).
- **P4 ARMED** — see next-session instruction below.

## Unaccepted candidate refs

- **ADR-0051 remains PROPOSED — owner H2 ratification pending**
  (non-delegable). Mechanical landing complete and live.

## Pending asynchronous work

- None dispatched. CI remains explicit-dispatch-only.

## Known inconsistencies and evidence gaps

- P4 (session-end background survival) uncharacterized — armed probe.
- qiven-context's devkit pin remains `d1d2a3a` (carries exec v2
  custody; bump to current devkit head is optional at next touching
  batch, not required for coverage).
- Standing (carried): session records for Qiven-v2 through Qiven-v5
  remain missing (Do not synthesize them); ContextView<ChatGPT, Jason>
  remains operational for ChatGPT sessions; governance audit due at
  five ADRs (0047/0048/0049/0050 accepted = four; ADR-0051 would be
  the fifth IF accepted); git-network routing still SUSPENDED at the
  router (owner reinstatement discretionary); RR-0 + accept-vanish
  regression open in the CA interval; additionalContext live injection
  owner-enable-gated.
- Process notes: no heredoc slips this session (the two sweep-class
  hook denials were complied with by re-routing reads, not detours);
  the P2 probe used an inline `python -c` for COMPUTE (allowed — not
  file authoring). ONE publication-discipline slip, disclosed and
  remediated: the checkpoint amendment recording the P4 probe identity
  (f17102e) was pushed to main BEFORE its gate ran — the gate was run
  immediately after the push and PASSED at that exact head (receipt
  context-local-f17102e...json), so the published head is proven, but
  the order violated the delegated-H2 receipt-first rule; this final
  checkpoint amendment follows the correct order (gate at head, then
  push).

## Next action

**Owner H2 decision point (paste-ready): ADR-0051 ratification**
(v25-amended; recommendation: 批准). Reply one of:

- **批准 ADR-0051** → set `status: accepted` + index + this checkpoint
  refresh; then the boundary stays: next-session P4 straggler check →
  MVP-4 real H1 rerun (`h1-kits/qiven-runtime/mvp4-h1/0.1.0-g6ebf6ff6`)
  → RR-0 → CA-1.
- **拒绝 ADR-0051** → roll back: revert qiven-context PR (this batch),
  revert devkit PR #32 (v4.1) and PR #31 (v4), revert runtime PR #59.
  The sibling custody rolls (foundation #20, math #12, draft #40,
  runtime #58) are ADR-0048 law under OBL-B9C1D3 and do NOT roll back.
  User-level files stay deleted regardless (owner action, outside the
  ADR; the mjs file remains on disk as inert evidence).

## Next-session cold-boot addendum (owner will close this session)

1. **FIRST ACTION — P4 straggler check**: this session leaves a
   bounded background probe running: task
   `exec_37ed0302-1581-4c2c-a6ca-f87f259ebfe3`, output file
   `C:/Users/61626/.zcode/cli/exec/sess_8fe5dfcb-02b8-417a-b2d9-5a50b2e1fb85/call_b42d9eb8d8f24550993d47cb-stdout.log`,
   started 2026-09-23T23:05Z (`echo P4-STRAGGLER-START && sleep 1800 &&
   echo P4-STRAGGLER-DONE`; natural completion ~23:35Z). Check
   `tasklist //FI "IMAGENAME eq sleep.exe"` (and bash.exe tree): if
   the probe survived session close → background tasks outlive their
   session; record it, sweeps stay exec-leased permanently, revisit
   ADR-0051 R1. If dead (and no DONE marker) → session-end kills
   background tasks; R1 resolves positively; record and close
   OBL-D5E6F7. If DONE present with a ~23:35Z timestamp → it survived
   close and completed naturally (same conclusion as survived).
2. ADR-0051 ratification state (owner reply above) drives the next
   boundary per state/current.md.
3. Long-running mode does NOT carry into the next session (fresh
   session needs explicit owner re-entry).
