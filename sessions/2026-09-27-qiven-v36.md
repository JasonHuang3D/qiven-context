# Qiven-v36 Session Checkpoint

## Session identity

ZCode Desktop foreground session, owner-marked new session 2026-09-27
("这个是新的session"). Cold boot per BOOTSTRAP; designation
`jason-extended-cognition` owner-invoked at turn 1 with the live-model
switch report (GLM-5.3 family, provider account `bigmodel-offpeak-idle-plan`,
per harness disclosure; variant and reasoning effort not introspectable
from inside, reported as such). **Long-running mode from turn 1** (owner
instruction: self-review the delivered MVP-4 simulated gate with the
subagent workflow, revise any gate-blocking obstacles until PASS, k=5
consecutive clean rounds imply the owner's MVP4_SIMULATION_ACCEPTED
acceptance, then prepare the H1 kit for the owner's manual post-turn
work; standing-delegated per-batch H2, H1-only stops).

## Exact current task

**EXECUTED TO A NECESSARY H1 (the standing reset ceiling):** the
owner-directed self-review loop over the v35 simulated gate ran three
full review+fix cycles, materially improved the artifact, and stopped
at the three-consecutive-reset ceiling without reaching the directed
five consecutive clean rounds — the pre-committed acceptance condition
did NOT fire; the acceptance adjudication returns to the owner.

1. Loop mechanics per `views/workflows/subagent-delegation.md`
   Self-review approval rounds: qiven-fresh-review workers with
   positionless self-contained 8-section briefs (contract inlined;
   pinned-source reference exports digest-attested under
   workspace `.generated-temp/v36-review-refs/`); one preflight canary
   (clean: no project knowledge, Write-only, nonce v36-a9f3);
   mechanical transcript scans after every round; triage under the
   standing materiality line (MEM-20260925T053000Z-C1D2E3); revisions
   in the MAIN session (write-bearing default).
2. Round 1 (15 findings): INV-10 claimed-not-asserted; B4-B7
   name/payload duplication; allow-cases under deny invariants;
   A7 weaker than its oracle; escaping tracebacks; receipt missing
   toolchain/self-digest; mediation scope not reconciled with lexical
   bypasses; the owner-named non-invocation cause classes
   (mid-turn hook-config change, OS/AV launch interception) neither
   covered nor recorded; kit-preflight weaker than the sim's evidence;
   oracle provenance entries missing; INV-9 client side unobservable.
   Fix batch 1: host `[open]` per-connection logging + S9 count case
   (old-fail PROVEN live: exactly S9 failed against pre-change
   binaries), INV-10 asserted at every registration site, governed
   Write matrix retargeted per-case, bindings corrected, A7
   journal+console assertions, duplicate merges (104→100), receipt
   self-digest+toolchain+tripwire, run-mode containment,
   complete-mediation scope both claim-blocking classes, catalogue
   oracles +3, preflight hardening. Published `367b44a`
   (branch `88d4233` gate PASS + merge-head gate PASS; receipts
   sim-88d423318301 + sim-367b44a6c9c5, 100/100/0).
3. Round 2 (8 findings): B11c residual binding; INV-15
   bound-without-effect-asserts on the Edit matrix/Bash legs/P16-P17;
   verify-receipt/old-fail-i4 containment; no short-name alias fixture;
   S8 handle omission; B1/B8 INV-13; mid-session-death shape unprobed.
   Fix batch 2: uniform INV-15 effect-freedom (redirect-target
   extraction for Bash), P22 short-name alias leg (this volume
   degenerates to the long form — deny branch, honestly labeled), C2
   post-shutdown typed-120 probe, containment for all modes, S8
   completion, design amendments 9-14. Published `1ef9667`
   (bc94607 + merge; receipts sim-bc9460726b98 +
   sim-1ef966713c29, 101/101/0).
4. Round 3 (11 findings, no P1): policy-fixture digest unbound;
   verify-receipt trusting counters over rows; argv form missing
   `--dump-stdin` (the REAL harness form per the kit CONFIG_TEMPLATE);
   plus P3 binding/name/budget/platform/containment residues. Fix
   batch 3: policy_fixture_digest + per-row consistency in
   verify-receipt, the real harness argv form with dump-landing
   assertion, per-event budgets (15/10/10 s) enforced in run_hook,
   P15/B21(B22→INV-19)/A13-A14 corrections, platform.platform(),
   `{python}` placeholders, catch-all typed handlers, preflight
   labeled degradation, design amendments 15-21. Published `ffb31c2`
   (receipt sim-ffb31c221c0d-20260926T175930Z.json, 101/101/0;
   gate:local PASS at the exact head). PROCESS SLIP recorded honestly:
   the batch-3 commit landed directly on main (branch ceremony
   skipped); the gate ran PASS at that exact head after the commit, so
   the published-head evidence holds.
5. Transcripts: all three worker scans clean (in-scope reads only,
   honest ACKs); the round-1 scan's 8 high hits were ONE
   false-positive class (marker matching the artifact's own
   governed-path fixture vocabulary) — the scanner's path markers are
   now anchored to the record-holding repository (probes:
   genuine fires, fixture forms excluded; rounds 2-3 scanned 0 hits).
6. Context side (this transaction): three lesson MEMs
   (C5D6E7 suite honesty, D6E7F8 simulator parity, E7F8A9 marker
   anchoring), r2r3 checklist invariant-map row updated (101 cases,
   INV-1..19) + the loop recorded as control-experiment evidence,
   OBL-B4C5D6 v36 progress, state records, this checkpoint.
7. H1 kit BUILT at the final head for the owner's manual
   installed-desktop work: `<workspace>/h1-kits/qiven-runtime/
   mvp4-h1/0.1.0-gffb31c22/` (GUIDE.md entry point; preflight BEFORE
   any UI enable; rollback + evidence-sealing scripts included; the
   kit backs up, never writes, the live workspace config).

## Accepted refs and evidence

- qiven-runtime main `ffb31c2` (pushed; history `1ef9667` ← `bc94607`
  ← `367b44a` ← `88d4233` ← aa2076e).
- Gate-grade receipts under qiven-runtime `.generated-temp/h1-sim/
  receipts/`: sim-88d423318301, sim-367b44a6c9c5, sim-bc9460726b98,
  sim-1ef966713c29, sim-ffb31c221c0d (all verify-receipt OK at their
  heads; final 101/101/0 at ffb31c2). Old-fail evidence: the S9
  dev receipt sim-aa2076e201d3-dev-20260926T172617Z (exactly S9
  failed, 'consumed 0 connections') + the preserved pre-fix binaries.
- qiven-workspace main `e6de2b6` (pushed; lock conformed to ffb31c2;
  the standing context→devkit pin [SPLIT] unchanged, WR-6 target).
- Transcript scans: `.generated-temp/v36-scan-{canary,r1,r1-final,
  r2,r3}.json`; reviewer transcripts `sess_subagent_agent_{c4c4bc9d,
  e7438479,eefbde02}`; canary `agent_a6758250`.
- v36 workflow log: `<workspace>/.generated-temp/workflow-logs/
  v36-mvp4-selfreview.md` (activation receipt, round ledger, tally,
  R2R3 ACC/OBS receipts, stash).

## The necessary H1 (session stops here)

The owner adjudicates:

1. **The `MVP4_SIMULATION_ACCEPTED` MVP-4 exit record** per ADR-0055,
   on the post-self-review evidence. The v36 loop stopped at the
   standing three-consecutive-reset ceiling (three full cycles each
   still warranting revision — though findings trended 15 → 8 → 11
   with the P1 gone and each round's findings materially smaller in
   scope); the owner-directed k=5 condition did not complete, so the
   turn-1 pre-commitment ("k=5做完后即代表我接受了") did not fire.
   Options: accept as-is on the improved evidence; direct ONE bounded
   further review pass (the ceiling forbids the orchestrator opening a
   fourth loop unilaterally — an owner direction for a bounded pass is
   the lawful form); or open the process question the ceiling raises.
2. **The owner's manual installed-desktop work**: the H1 kit is ready
   (0.1.0-gffb31c22, GUIDE.md; preflight first, UI enable is owner
   hands, rollback included). The standing
   `INSTALLED_DESKTOP_EXECUTION_UNVERIFIED` residual is exactly what
   this manual work addresses.
3. **The next boundary** (unchanged from v35): RR-0 → CA-1 interleave,
   or the WR-5/WR-6 pre-wave.

## Unaccepted candidate refs

- None open in qiven-runtime (all three amendment batches published
  under delegated H2 with gate PASS at their published heads).
- THIS context transaction (MEMs + checklist + OBL/state riders + this
  checkpoint + the scanner anchoring fix) rides the same delegated H2:
  branch → context-tools gate PASS at the exact head → merge → push.

## Pending asynchronous work

- None running. (All gates completed; no straggler builds.)

## Known inconsistencies and evidence gaps

- The standing qiven-context devkit pin (d1d2a3a) vs lock devkit node
  typed [SPLIT] — unchanged, WR-6 target.
- The batch-3 branch-ceremony slip (direct-to-main commit with the
  gate run at that head after the commit) — recorded above; evidence
  chain intact, process note for the next batch.
- The rig's detector-limits list is honest coverage-gap recording
  (dot-segment, double separator, junction/reparse, backslash root
  form; P22's short-name branch on this volume degenerates to the long
  form) — MVP-7 dogfood territory, not claimed as mediation.
- Carried (literal, gate-checked): session records for Qiven-v2
  through Qiven-v5 remain missing (do not synthesize them); ZCode
  version-drift caveat stands; the scanner taxonomy is English-tuned.

## Cost honesty (subagent consumption)

- Canary (general-purpose): 28.8k tokens, 1 tool call.
- Review rounds (qiven-fresh-review): r1 458k / r2 400k / r3 493k
  subagent tokens (21/20/17 tool uses). Orchestrator input cost: the
  full cold-boot corpus + three triage/fix/publication cycles in the
  main context.

## Standing continuity note

Canonical records are authoritative; this checkpoint is bounded
continuity evidence only. The PR-6 interim checklist bound this batch
(activation receipt filled from live reads at window open; no compact
boundary crossed; ACC/OBS receipts in the workflow log).

## Next action

Session STOPS at the owner H1 above. A fresh session cold-boots to the
owner's adjudication + next-boundary choice (state/current.md Next
boundary; active-work.yaml current_candidate).
